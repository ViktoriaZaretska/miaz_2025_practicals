# Завдання 2 — Обробка Excel

Файл: `excel_processor.py`

Опис:
- Читає `data.xlsx`.
- Фільтрує рядки (приклад: Score > 80).
- Записує `filtered_data.xlsx`.

Запуск:
```bash
python excel_processor.py
